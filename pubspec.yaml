name: app_conductor
description: "Zumbeo — App Conductor (recibe pedidos, cobra y liquida comisiones)."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.8.0

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  provider: ^6.1.5+1
  get_it: ^9.2.1
  dio: ^5.10.0
  flutter_secure_storage: ^10.3.1
  flutter_map: ^8.3.0
  latlong2: ^0.9.1
  geolocator: ^14.0.2
  geocoding: ^4.0.0
  image_picker: ^1.2.1
  # Recompresión de la evidencia de entrega antes de subirla (ver
  # data/services/imagen_compresor.dart). Dart puro, sin código nativo ni
  # configuración de plataforma. `dart:ui` no servía: decodifica, pero solo
  # codifica PNG, y un PNG de una foto pesa más que el JPEG de origen.
  image: ^4.8.0
  stomp_dart_client: ^3.0.1
  firebase_core: ^4.11.0
  firebase_messaging: ^16.4.1
  # ── Solo en la app conductor ────────────────────────────────────────────────
  # Hasta aquí los dos pubspec eran gemelos, y esa simetría era una comodidad
  # real al portar código. Se rompe a conciencia: la app cliente no tiene ninguna
  # razón para despertar la pantalla bloqueada ni para pedir exención de batería.
  #
  # El aviso de oferta a pantalla completa (fullScreenIntent + categoría llamada)
  # no se puede pedir desde el payload de FCM: hay que construir la notificación
  # en el dispositivo, y eso es lo que hace este paquete.
  flutter_local_notifications: ^19.4.2
  # Exención de la optimización de batería. En los teléfonos de gama media que
  # dominan el mercado local el sistema mata el proceso al minimizar la app, y
  # sin la exención no llegan ni el push ni el sondeo.
  permission_handler: ^12.0.1
  google_sign_in: ^7.2.0
  sign_in_with_apple: ^7.0.1
  url_launcher: ^6.3.2
  # Versión instalada real (para el banner de versión nueva): leerla del paquete
  # y no de un --dart-define evita reportar una versión falsa en builds manuales.
  package_info_plus: ^8.1.1
  # Marca, modelo y versión del sistema, para el reporte de feedback y para
  # identificar el equipo en el panel. Se obtiene, no se le pregunta a nadie.
  device_info_plus: ^12.2.0
  intl: ^0.20.2
  shared_preferences: ^2.5.3
  go_router: ^17.0.0
  # Caché en disco de los tiles del mapa (ver data/services/map_tile_cache.dart).
  # Las mismas versiones que app_cliente: las dos apps comparten infraestructura
  # y divergir en el paquete de mapas es cómo se acaban viendo distintas.
  flutter_map_cache: ^1.5.2
  dio_cache_interceptor: ^3.5.1
  dio_cache_interceptor_file_store: ^1.2.3
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^5.0.0
  mocktail: ^1.0.5
  flutter_launcher_icons: ^0.14.4

# Genera los iconos de Android/iOS desde la cabeza de la mascota sobre navy de
# marca. El lockup completo (emblema + "Zumbeo CONDUCTOR") no sirve de icono:
# a 48px el texto es una mancha y la moto de perfil se deshace.
# Regenerar con: dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/images/icono.png"
  min_sdk_android: 23
  adaptive_icon_background: "#17293D"
  # Capa frontal aparte: va dentro de la zona segura (66 de 108 dp) para que la
  # máscara circular del launcher no le corte el casco.
  adaptive_icon_foreground: "assets/images/icono_foreground.png"

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/images/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package


